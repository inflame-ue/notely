name: cd

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the Code
        uses: actions/checkout@v4

      - name: Setup the Go Toolchain
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.0"

      - name: Build the App
        run: ./scripts/buildprod.sh

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ secrets.GCP_CREDENTIALS }}'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v3

      - name: Build the docker image and push it Google AR
        run: gcloud builds submit --tag us-central1-docker.pkg.dev/notely-498310/notely-ar-repo/inflameue/notely:latest

      - name: Deploy to Cloud Run
        run: gcloud run deploy notely --image us-central1-docker.pkg.dev/notely-498310/notely-ar-repo/inflameue/notely:latest --region us-central1 --allow-unauthenticated --project notely-498310 --max-instances=4
